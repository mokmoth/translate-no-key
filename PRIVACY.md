# Privacy

## 本地处理

应用代码不包含分析、遥测、广告、第三方字体或网络请求。输入、输出与映射在当前浏览器页面内存中处理，不写入 `localStorage`、`sessionStorage`、IndexedDB 或 Cookie。

剪贴板只在用户点击“粘贴”或“复制”后访问；导入的映射文件只在页面内读取。

## 映射包含明文

映射表为了还原占位符，必然保存原始敏感值。页面中的“查看映射”会显示明文，导出的映射 JSON 也包含明文，不是加密备份。它应按密码文件处理：只保存在可信设备上，不上传、不提交到 Git，使用后安全清理。

## 生命周期与丢失风险

刷新或关闭页面会清除内存中的映射；“清空映射”会在确认后立即清除当前映射。这些操作不可撤销，未导出的映射丢失后无法自动还原已有占位符。

## 外部边界

如果通过静态服务器托管，服务器可能记录页面请求的 IP、时间和 User-Agent，但应用不会把粘贴的配置作为请求发送。浏览器扩展、剪贴板历史、输入法、屏幕录制、恶意宿主页面或操作系统功能可能观察页面内容，均不由本项目控制。

自动检测是 best-effort。分享脱敏结果前必须人工检查是否仍含敏感信息。
